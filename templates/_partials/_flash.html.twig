        {% for label, messages in app.flashes() %}
            {% for message in messages %}
                <div class="alert alert-alert-{{ label }}">
                    {{ message|join('.') }}
                </div> 
            {% endfor %}
        {% endfor %}
