<form method="post" action="{{ path('admin.crews.delete', {'id': crew.id}) }}" onsubmit="return confirm('Confirmer la suppression de ce concurrent');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ crew.id) }}">
    <button class="btn">Delete</button>
</form>
